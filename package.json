{
  "name": "wedding-media-uploader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Premium mobile-first wedding guest photo & video uploader for Cloudflare R2 or Google Drive",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:vercel": "vercel dev",
    "build": "npm run build --prefix client",
    "build:all": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "import-drive": "tsx scripts/import-drive.ts",
    "migrate-drive-to-r2": "tsx scripts/migrate-drive-to-r2.ts",
    "flatten-r2-keys": "tsx scripts/flatten-r2-keys.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1072.0",
    "@aws-sdk/s3-request-presigner": "^3.1072.0",
    "@supabase/supabase-js": "^2.49.8",
    "dotenv": "^16.4.7",
    "google-auth-library": "^9.15.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@vercel/node": "^5.3.0",
    "concurrently": "^9.1.2",
    "tsx": "^4.19.3",
    "vercel": "^42.1.0"
  }
}
