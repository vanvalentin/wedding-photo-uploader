# Secret — server only, never expose in the browser (Settings → API → Secret key)
SUPABASE_SECRET_KEY=sb_secret_your_key_here

# Legacy service_role JWT still works as fallback:
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
